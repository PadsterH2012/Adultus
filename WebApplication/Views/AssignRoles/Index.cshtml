@model  PagedList.IPagedList<WebApplication.Models.HomeViewModel>
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
@Styles.Render("~/Content/css")
<table class="table">
        @foreach (var role in Model.Single().aspUserClaim)
        {
            <tr>
                <th>
                    @Html.DisplayFor(model => role.ClaimType)
                </th>
                <th>
                    @Html.DisplayFor(model => role.ClaimValue)
                </th>
                <th>
                    @Html.DisplayFor(model => role.UserId)
                </th>
            </tr>
        }
</table>
   @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
